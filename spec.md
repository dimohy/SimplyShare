# SimplyShare - 사양서 (Specification)

> **내부 네트워크용 텍스트 및 파일 공유 애플리케이션**
> 버전: 1.0 | 작성일: 2026-02-11

---

## 1. 개요

SimplyShare는 별도의 서버 없이 내부 네트워크(LAN) 환경에서 텍스트와 파일을 간편하게 공유하는 P2P 데스크톱 애플리케이션이다.

### 1.1 핵심 가치
- **심플함**: 설치 후 바로 사용 가능 (서버 설정, 계정 생성 불필요)
- **빠름**: 내부 네트워크 직접 전송으로 외부 서버 경유 없이 최대 속도
- **안전함**: 전송 데이터 암호화로 네트워크 스니핑 방지
- **가벼움**: ReadyToRun 단일 EXE, 별도 설치 불필요

### 1.2 기술 스택
| 항목 | 기술 |
|------|------|
| 프레임워크 | .NET 10 |
| 언어 | C# 14 |
| UI | WPF |
| 배포 | ReadyToRun + Self-Contained 단일 파일 (EXE) |
| 통신 | TCP (데이터 전송) + UDP (장치 발견) |
| 암호화 | ECDH 키 교환 + AES-256-GCM |

---

## 2. 기능 요구사항

### 2.1 장치 발견 (Device Discovery)
- **UDP 브로드캐스트/멀티캐스트**로 같은 네트워크 내 SimplyShare 인스턴스 자동 검색
- **네트워크 대역 설정**: `192.168.100.*` 형식으로 탐색 범위 제한
  - 여러 대역 추가/제거 가능
  - 설정되지 않으면 현재 네트워크 서브넷 자동 감지
- 주기적으로 Heartbeat 전송하여 온라인/오프라인 상태 실시간 반영
- 각 장치는 **닉네임**으로 식별 (최초 실행 시 설정)

### 2.2 텍스트 공유
- **클립보드 자동 감지**: 클립보드에 텍스트 복사 시 자동 인식
- **버튼 클릭으로 전송**: 감지된 텍스트를 선택한 대상에게 전송
- 수신된 텍스트는 알림과 함께 표시, 클립보드에 복사 가능
- 전송 대상:
  - **특정 장치 1:1 전송**: 장치 목록에서 선택
  - **전체 브로드캐스트**: 네트워크 내 모든 장치에 일괄 전송

### 2.3 파일 공유
- **드래그 앤 드롭**: 앱 창에 파일/폴더를 드래그하여 전송
- **복수 파일/폴더 동시 전송** 지원
- **대용량 파일 지원**: GB 단위 파일 전송, 진행률 표시
- 전송 대상:
  - **특정 장치 1:1 전송**: 장치 목록에서 선택
  - **전체 브로드캐스트**: 네트워크 내 모든 장치에 일괄 전송
- 수신 파일 저장: **사용자 다운로드 폴더** (기본값)

### 2.4 수신 동작
- 파일/텍스트 수신 시 **수락/거부 확인** 팝업 표시
- 수락 시 파일 저장 또는 텍스트 표시
- 거부 시 전송자에게 거부 알림 전달

### 2.5 암호화
- **ECDH (Elliptic Curve Diffie-Hellman)** 키 교환으로 세션 키 생성
- **AES-256-GCM** 대칭키 암호화로 모든 데이터(텍스트/파일) 암호화 전송
- 각 P2P 연결마다 고유 세션 키 사용
- 네트워크 스니핑으로부터 데이터 보호

---

## 3. UI/UX 요구사항

### 3.1 메인 화면 (드래그앤드롭 중심)
- **장치 목록 패널**: 온라인 장치 표시 (닉네임 + 상태)
  - 장치 선택/전체 선택 토글
- **드롭 영역**: 파일/폴더를 드래그하여 놓는 중앙 영역
- **클립보드 패널**: 감지된 클립보드 텍스트 미리보기 + 전송 버튼
- **전송/수신 상태 패널**: 현재 진행 중인 전송 목록 (진행률 바)

### 3.2 시스템 트레이
- **트레이 상주**: 창을 닫아도 트레이에서 계속 동작
- 최소화 시 트레이로 이동
- 트레이 아이콘 더블클릭으로 창 복원
- 트레이 우클릭 메뉴:
  - 열기
  - 설정
  - 종료

### 3.3 알림
- **Windows Toast 알림**: 파일/텍스트 수신 시 시스템 알림
- 알림 클릭 시 수락/거부 팝업 또는 앱 창 활성화

### 3.4 설정 화면
- 닉네임 변경
- 네트워크 대역 설정 (추가/제거)
- 수신 파일 저장 경로 변경
- 시작프로그램 등록 여부

### 3.5 최초 실행
- 닉네임 입력 화면 (필수)
- 네트워크 대역 설정 (선택, 미설정 시 자동 감지)

---

## 4. 네트워크 프로토콜

### 4.1 장치 발견 (UDP)
```
포트: 52525 (기본값, 설정 가능)
프로토콜: UDP 브로드캐스트

메시지 형식 (JSON):
{
  "type": "discovery" | "heartbeat" | "goodbye",
  "nickname": "사용자닉네임",
  "deviceId": "고유ID",
  "port": 52526,         // TCP 수신 포트
  "version": "1.0"
}
```

### 4.2 데이터 전송 (TCP)
```
포트: 52526 (기본값, 설정 가능)
프로토콜: TCP

연결 흐름:
1. ECDH 키 교환 → 세션 키 생성
2. 전송 요청 (타입: text | file | files)
3. 수신자 수락/거부 응답
4. 수락 시 AES-256-GCM 암호화된 데이터 전송
5. 전송 완료 확인
```

### 4.3 메시지 프레임
```
[4 bytes: 메시지 길이(Big Endian)] + [N bytes: 암호화된 페이로드]

페이로드 구조 (복호화 후 JSON):
{
  "type": "key-exchange" | "transfer-request" | "transfer-response" | "data" | "complete" | "error",
  "payload": { ... }
}
```

---

## 5. 비기능 요구사항

### 5.1 성능
- 장치 발견: 앱 시작 후 3초 이내에 네트워크 내 장치 표시
- 파일 전송: 네트워크 대역폭의 80% 이상 활용
- 메모리: 유휴 시 50MB 이하, 전송 중 버퍼 제외 100MB 이하
- 시작 시간: 2초 이내 (ReadyToRun)

### 5.2 안정성
- 전송 중 연결 끊김 시 자동 정리 및 알림
- 대용량 파일 전송 시 스트리밍 방식 (전체 메모리 로드 하지 않음)
- 동시 다중 전송 지원

### 5.3 호환성
- Windows 10 이상
- x64 아키텍처

### 5.4 배포
- ReadyToRun + Self-Contained 단일 파일 배포
- WPF는 Native AOT 미지원으로 ReadyToRun 방식 채택
- JSON 직렬화는 Source Generator 기반 (리플렉션 최소화)
- .NET 런타임 포함 단일 EXE

---

## 6. 프로젝트 구조 (예상)

```
SimplyShare/
├── src/
│   └── SimplyShare/
│       ├── SimplyShare.csproj
│       ├── App.xaml / App.xaml.cs
│       ├── MainWindow.xaml / MainWindow.xaml.cs
│       ├── Core/
│       │   ├── Discovery/          # 장치 발견 (UDP)
│       │   ├── Transfer/           # 파일/텍스트 전송 (TCP)
│       │   ├── Crypto/             # 암호화/복호화
│       │   └── Clipboard/          # 클립보드 감시
│       ├── ViewModels/             # MVVM ViewModel
│       ├── Views/                  # XAML Views
│       ├── Models/                 # 데이터 모델
│       ├── Services/               # DI 서비스
│       └── Converters/             # WPF 값 변환기
├── docs/
│   ├── spec.md
│   └── agents.md
└── README.md
```

---

## 7. 미래 확장 가능성 (v2+)
- 이미지 미리보기
- 전송 히스토리
- 폴더 압축 전송 옵션
- 다크 모드
- 자동 업데이트
